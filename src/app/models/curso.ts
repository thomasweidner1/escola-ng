export class Curso {
    // id: number = 0;
    // nome: string = "";
    // sigla: string = "";

    // constructor(id: number, nome: string, sigla: string){
    //     this.id = id;
    //     this.nome = nome;
    //     this.sigla = sigla;
    // }

    constructor(
        public id: number,
        public nome: string,
        public sigla: string     
    ){}
}
