import { FormatarCpfPipe } from './formatar-cpf.pipe';

describe('FormatarCpfPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatarCpfPipe();
    expect(pipe).toBeTruthy();
  });
});
