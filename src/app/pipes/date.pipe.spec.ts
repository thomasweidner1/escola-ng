import { DataHoraCustomizadaPipe } from './data-hora-customizada.pipe';

describe('DataHoraCustomizadaPipe', () => {
  it('create an instance', () => {
    const pipe = new DataHoraCustomizadaPipe();
    expect(pipe).toBeTruthy();
  });
});
